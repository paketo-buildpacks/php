A basic PHP app that features Composer usage. Composer is needed to make sure
that the `require '../vendor/autoload.php';` runs properly.

Can be used with any of the web servers supported in this buildpack.
